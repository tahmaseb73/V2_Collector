name: V2_Collector

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */4 * * *'

jobs:
  HiN-VPN:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4 # به‌روزرسانی به نسخه جدیدتر

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'

      - name: Debug Directory Structure
        run: |
          pwd
          ls -la /home/runner/work/V2_Collector/V2_Collector/
          ls -la .
          # اگر می‌دانید $directory چیست، مسیر را اینجا چک کنید
          ls -la your_folder || echo "Directory your_folder not found"

      - name: Execute PHP Script
        run: |
          php base.php
          git config --global user.email "yebekhe@gmail.com"
          git config --global user.name "yebekhe"
          git config credential.helper store
          git add -A
          git commit -m "🚀 Last Run - $(TZ='Asia/Tehran' date '+%Y-%m-%d %H:%M:%S')"
          git push
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
